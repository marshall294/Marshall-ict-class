const whatsappNumber = "2348102341648";

function requestService(serviceType) {
  const message = `Hello, I'm interested in ${serviceType}. Please provide more info.`;
  const url = `https:                                                               
  window.open(url, '_blank');
}

function subscribe() {
  const email = document.getElementById('email').value;
  if (email) {
    const message = `//wa.me/${whatsappNumber}?text=${encodeURIComponent(message)}`;
  window.open(url, '_blank');
}

function subscribe() {
  const email = document.getElementById('email').value;
  if (email) {
    const message = `New subscription: ${email}`;
    const url = `https://wa.me/${whatsappNumber}?text=${encodeURIComponent(message)}`;
    window.open(url, '_blank');
    document.getElementById('email').value = '';
  }
}

